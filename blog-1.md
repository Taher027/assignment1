## In TypeScript `any` labeled as type safety hole, But `unknown` is the safer choice for handling unpredictable data

TypeScrpt gives us type safety. It catches the mistake before running the code. We can easily define the type of any data. But some times, when we fetch data we don't know the response data type. In that case, we use `any`or `undefined`.
